<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration
    ────────────────────────────────────────────────────────────────────────────
    Production: backend should be HTTPS → no changes needed.
    Development: if your mock backend runs over plain HTTP on your LAN,
                 add your PC's IP to the <domain> list below.

    Referenced from AndroidManifest.xml:
      android:networkSecurityConfig="@xml/network_security_config"
-->
<network-security-config>

    <!-- Trust system CAs for all HTTPS traffic (default — production) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        DEV ONLY: allow plain HTTP to your local mock backend.
        Replace 192.168.1.100 with your actual development machine IP.
        Remove this block (or set cleartextTrafficPermitted="false") for production builds.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.100</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>  <!-- Android emulator host -->
    </domain-config>

</network-security-config>
